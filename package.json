{
  "name": "erp-fullstack",
  "private": true,
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "dev": "npm-run-all --parallel server client"
  }
}
